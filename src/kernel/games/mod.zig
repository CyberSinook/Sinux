const std = @import("std");

pub const pacman = @import("pacman.zig");
pub const game_console = @import("pacman_console.zig");

pub const Games = struct {
    pub fn init() !void {
        // Initialize games subsystem
    }
};
